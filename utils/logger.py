def log(mensaje):
    """
    Imprime un mensaje en la consola con un prefijo [LOG].
    Útil para depuración y seguimiento de eventos.
    
    Args:
        mensaje (str): El mensaje a registrar.
    """
    print(f"[LOG] {mensaje}")
